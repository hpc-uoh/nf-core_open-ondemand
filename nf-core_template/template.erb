---
cluster: "kutral"

attributes:

  num_cores:
    widget: "hidden_field"
    label: "Número de cores"
    required: true
    value: 1
    min: 1

  num_memory:
    widget: "hidden_field"
    label: "Memoria en GB"
    required: true
    value: 1
    min: 1

  workdir:
    label: "Working Directory"
    widget: "path_selector"
    directory: "/mnt/beegfs/home/"
    required: false
    label: Working directory
    favorites:
        - /mnt/beegfs/home/
        - /mnt/beegfs/labs/
    
  resume:
    label: Resume
    widget: check_box
    checked_value: 1
    unchecked_value: 0
    help: |
           Resume previous run if found

  tower_access_token:
    widget: "text_field"
    required: false
    label: "TOWER_ACCESS_TOKEN (Optional)"
    help: "Your TOWER_ACCESS_TOKEN used to monitor runs on [Nextflow Tower](https://seqera.io)."

