---
batch_connect:
  template: basic
  conn_params:
    - working_path 

script:
  email_on_started: true
  email_on_terminated: true
  email: "<%= `ldapsearch -x -LLL -H ldap://172.16.105.154 -b "uid=#{ENV['USER']},ou=people,dc=srv,dc=world" mail | awk -F": " '/^mail:/ {print $2}'`.strip %>"
  job_name: "OnDemand/nf-core_PIPELINE"
  native:
    - "-n 1"
    - "--mem=1G"   
